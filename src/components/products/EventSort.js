import React from 'react';
import {
    StyleSheet,
    Text,
    Image,
    View,
    Slider,
    ScrollView,
    ActivityIndicator,
    ListView,
    TouchableOpacity,
} from 'react-native';
import Global from '../Global';

class EventSort extends React.Component {
  constructor(props) {
    super(props);
  }

  render() {
    return(
      <View>
        <Text>Filtres events</Text>
      </View>
    );
  }

}


const styles = StyleSheet.create({

});

export default EventSort;
