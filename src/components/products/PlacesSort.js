import React from 'react';
import {
    StyleSheet,
    Text,
    Image,
    View,
    Slider,
    ScrollView,
    ActivityIndicator,
    ListView,
    TouchableOpacity,
} from 'react-native';
import Global from '../../Global';

class PlacesSort extends React.Component {
  constructor(props) {
    super(props);
  }

  render() {
    return(
      <View>
        <Text>Filtres PLACES (musees, jardins, etc)</Text>
      </View>
    );
  }

}


const styles = StyleSheet.create({

});

export default PlacesSort;
